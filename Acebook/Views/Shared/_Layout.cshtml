<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - acebook</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="flex flex-col min-h-screen">
    @using acebook.Models
    @using System.Linq

    @{
        int? userId = Context.Session.GetInt32("user_id");
        string? userName = Context.Session.GetString("user_name");
        var currentScope = (ViewData["Scope"] as string) ?? "all";
        string Label(string s) => s switch
        {
            "users" => "People",
            "posts" => "Posts",
            "comments" => "Comments",
            _ => "All categories"
        };

            string imagePath; 
        if (!string.IsNullOrEmpty(Context.Session.GetString("user_profile_picture")))
        {
            imagePath = Context.Session.GetString("user_profile_picture");
        }
        else
        {
            imagePath = "/images/Placeholder.png";
        }
    }
    <header>
            <nav class="fixed top-0 py-2 left-0 w-full bg-teal-600 z-50">
                <div id="navbar" class="mx-auto max-w px-2 sm:px-6 lg:px-8 relative flex h-16 justify-between">
                        <div class="flex flex-1 items-center justify-center sm:justify-start">
                            <div class="flex shrink-0 items-center">
                                
                                <!-- SeaShark Logo -->
                                <a asp-action="Index" asp-controller="Home" method ="get">
                                    <img src="@Url.Content("~/images/light_grey.png")"
                                    alt="SeaShark Logo" class="h-14 w-auto"  />
                                
                                </a>
                            </div>
                        
                        <!--  If statement for different views if user is/isn't signed in  -->
                            @if (userId != null)
                            {
                                <div class="hidden sm:ml-6 sm:block">

                        
                                    <div class="flex items-center justify-center gap-0">
                                    <!-- ONE form that submits both scope + SearchString -->
                                    <form method="get" action="/Search" class="flex items-center max-w-lg w-full">
                                        <div class="flex relative">
                                        <!-- Dropdown toggle button -->
                                        <button id="dropdown-button"
                                                data-dropdown-toggle="dropdown2"
                                                type="button"
                                                class="shrink-0 z-10 inline-flex items-center py-2.5 px-4 text-sm font-medium text-center text-gray-900 bg-gray-100 border border-gray-300 rounded-s-lg hover:bg-gray-200 focus:ring-4 focus:outline-none focus:ring-gray-100">
                                            <span id="scope-label">@Label(currentScope)</span>
                                            <svg class="w-2.5 h-2.5 ms-2.5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 10 6">
                                            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 4 4 4-4"/>
                                            </svg>
                                        </button>

                                        <!-- Hidden input the form will submit -->
                                        <input type="hidden" name="scope" id="scope-input" value="@currentScope" />

                                        <!-- Flowbite dropdown menu (positioned under the button) -->
                                        <div id="dropdown2"
                                            class="z-10 hidden absolute top-full left-0 bg-white divide-y divide-gray-100 rounded-lg shadow-sm w-44">
                                            <ul class="py-2 text-sm text-gray-700" aria-labelledby="dropdown-button">
                                            <li><button type="button" class="scope-option inline-flex w-full px-4 py-2 hover:bg-gray-100" data-scope="users" data-testid="user-test-button">People</button></li>
                                            <li><button type="button" class="scope-option inline-flex w-full px-4 py-2 hover:bg-gray-100"  data-scope="posts" data-testid="post-test-button">Posts</button></li>
                                            <li><button type="button" class="scope-option inline-flex w-full px-4 py-2 hover:bg-gray-100" data-scope="comments" data-testid="comment-test-button">Comments</button></li>
                                            <li><button type="button" class="scope-option inline-flex w-full px-4 py-2 hover:bg-gray-100" data-scope="all" data-testid="all-test-button">All</button></li>
                                            </ul>
                                        </div>

                                        <!-- Search input (use placeholder, not SearchString=) -->
                                        <div class="relative w-full">
                                            <input type="search"
                                                id="search-input"
                                                name="SearchString"
                                                class="block p-2.5 w-full z-20 text-sm text-gray-900 bg-gray-50 rounded-e-lg border-s-gray-50 border-s-2 border border-gray-300 focus:outline-none focus:ring-0 focus:border-teal-600 "
                                                placeholder="@(ViewData["SearchPlaceholder"] ?? "Search...")"
                                                required />

                                            <button type="submit" data-testid="search-submit"
                                                    class="absolute top-0 end-0 p-2.5 text-sm font-medium h-full bg-gray-200 rounded-e-lg border border-gray-200 hover: text-white focus:ring-4 focus:outline-none focus:ring-white">
                                            <svg class="w-4 h-4" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 20 20">
                                                <path stroke="black" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                    d="m19 19-4-4m0-7A7 7 0 1 1 1 8a7 7 0 0 1 14 0Z"/>
                                            </svg>
                                            <span class="sr-only">Search</span>
                                            </button>
                                        </div>
                                        </div>
                                    </form>
                                    </div>
                                    </div>
                            <!-- Profile dropdown -->
                                <div>
                                    <button data-testid="dropdown1" id="dropdownDefaultButton" data-dropdown-toggle="dropdown1" class="absolute top-1/2 -translate-y-1/2 right-6 rounded-full ring-offset-0 focus:ring-4 focus:outline-none focus:ring-teal-900" type="button">
                                        <img 
                                            @* src="@Url.Content("@imagePath")"  *@
                                            src = "@imagePath"
                                            alt="User menu" 
                                            class="h-12 w-12 rounded-full border-2 border-white"
                                        />
                                    </button>

                                    <!-- Dropdown menu -->
                                    <div id="dropdown1" class="z-10 hidden bg-white divide-y divide-gray-100 rounded-lg shadow-sm w-44 ">
                                        <ul class="py-2 text-sm text-gray-700 " aria-labelledby="dropdownDefaultButton">
                                        <li>
                                            <a asp-controller="Users" asp-action="Index" asp-route-id="@userId" method="get" class="block px-4 py-2 hover:bg-gray-100" id="MyProfile">My Profile</a>
                                        </li>
                                        <li>
                                            <a href="#" class="block px-4 py-2 hover:bg-gray-100">Account Settings</a>
                                        </li>
                                        <li>
                                            <form asp-controller="Sessions" asp-action="Signout" method="post" class="d-inline">
                                            <button type="submit" data-testid="signout" id="signout" class="block w-full text-left px-4 py-2 appearance-none hover:bg-gray-100">Sign Out</button>
                                            </form>
                                        </li>
                                        </ul>
                                    </div>
                                </div>
                            }
                            else
                            {<a>
                                <a class="absolute right-2 flex items-center rounded-md px-3 py-2 text-sm font-medium text-gray-300 hover:bg-white/15 hover:text-white" asp-action="New" asp-controller="Users" method ="get">Sign Up</a>
                            </a>   
                            }

                        
                        </div>
                </div>

            </nav>



            
    </header>
        <main role="main" class="container mx-auto flex-grow pb-3 mb-8 pt-24">
            @RenderBody()
        </main>


    <footer class="bg-white rounded-lg shadow-sm m-4">
    <div class="w-full mx-auto max-w-screen-xl p-4 md:flex md:items-center md:justify-between">
        <span class="text-sm text-gray-500 sm:text-center">© 2025 <a asp-action="Index" asp-controller="Home" method ="get" class="hover:underline">SeaShark™</a>. All Rights Reserved.
    </span>
    <ul class="flex flex-wrap items-center mt-3 text-sm font-medium text-gray-500 sm:mt-0">
        <li>
            <a asp-action="Privacy" asp-controller="Home" method ="get" class="hover:underline me-4 md:me-6"> Privacy Policy</a>
        </li>
    </ul>
    </div>
</footer>
    @* <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script> *@
    <script src="https://unpkg.com/flowbite@2.5.1/dist/flowbite.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)


    <button id="back-to-top"
        class="fixed bottom-6 right-6 bg-teal-600 text-white p-3 rounded-full shadow-2xl hover:bg-teal-500 mb-10 transition transition-opacity duration-200 opacity-0 pointer-events-none">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24"
            stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M5 15l7-7 7 7" />
</svg>

    </button>

</body>

</html>

<!-- NOTIFICATION ICON -->
                            @* <button type="button"
                                class="relative rounded-full p-1 text-gray-400 focus:outline-2 focus:outline-offset-2 focus:outline-indigo-500">
                                <span class="absolute -inset-1.5"></span>
                                <span class="sr-only">View notifications</span>
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"
                                    data-slot="icon" aria-hidden="true" class="size-6">
                                    <path
                                        d="M14.857 17.082a23.848 23.848 0 0 0 5.454-1.31A8.967 8.967 0 0 1 18 9.75V9A6 6 0 0 0 6 9v.75a8.967 8.967 0 0 1-2.312 6.022c1.733.64 3.56 1.085 5.455 1.31m5.714 0a24.255 24.255 0 0 1-5.714 0m5.714 0a3 3 0 1 1-5.714 0"
                                        stroke-linecap="round" stroke-linejoin="round" />
                                </svg>
                            </button> *@

<!-- Code to get name/id from session -->
  @* <ul class="navbar-nav ms-auto">
                @if (userId != null && userName != null)
                {
                    <li class="nav-item">
                        <span class="navbar-text me-2">Signed in as @userName</span>
                    </li>
                    <li class="nav-item">
                        <form asp-controller="Sessions" asp-action="Signout" method="post" class="d-inline">
                            <button type="submit" id="signout" class="btn btn-link nav-link text-dark"
                                style="border:none; padding:0;">Sign Out</button>
                        </form>
                    </li>
                }
                else
                {
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-controller="Users" asp-action="New">Sign Up</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-controller="Sessions" asp-action="New">Sign In</a>
                    </li>
                }
            </ul> *@


<script>
  (function(){
    const scopeInput = document.getElementById('scope-input');
    const scopeLabel = document.getElementById('scope-label');
    const labelMap = { all: 'All categories', users: 'People', posts: 'Posts', comments: 'Comments' };

    document.querySelectorAll('.scope-option').forEach(btn => {
      btn.addEventListener('click', (e) => {
        e.preventDefault(); // safety: never submit
        const scope = btn.getAttribute('data-scope');
        scopeInput.value = scope;
        scopeLabel.textContent = labelMap[scope] || 'All categories';

        // hide Flowbite menu manually (Flowbite toggles the 'hidden' class)
        const menu = document.getElementById('dropdown2');
        if (menu && !menu.classList.contains('hidden')) menu.classList.add('hidden');
      });
    });
  })();
</script>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const backToTop = document.getElementById('back-to-top');

    // Show/hide button when scrolling
    window.addEventListener('scroll', () => {
      if (window.scrollY > 0) {
        backToTop.classList.remove('opacity-0', 'pointer-events-none');
        backToTop.classList.add('opacity-100');
      } else {
        backToTop.classList.add('opacity-0', 'pointer-events-none');
        backToTop.classList.remove('opacity-100');
      }
    });

    // Smooth scroll to top when clicked
    backToTop.addEventListener('click', () => {
      window.scrollTo({
        top: 0,
        behavior: 'smooth'
      });
    });
  });
</script>


